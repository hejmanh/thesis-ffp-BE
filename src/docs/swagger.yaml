openapi: 3.0.0
info:
  title: FFP API
  version: 1.0.0

servers:
  - url: http://localhost:3000

paths:
  /api/health:
    get:
      summary: Health check
      responses:
        200:
          description: OK